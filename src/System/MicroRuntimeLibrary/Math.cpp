#define WIN32_LEAN_AND_MEAN
#define WIN32_EXTRA_LEAN
#include <windows.h>

extern "C"
{
    int _fltused = 0;/*

    #pragma function(_CIsqrt)
    double sqrt(double val)
    {
        return 0;
    }

    #pragma function(acos)
    double acos(float val)
    {
        return 0;
    }

    #pragma function(cos)
    double cos(float val)
    {
        return 0;
    }

    #pragma function(sin)
    double sin(float val)
    {
        return 0;
    }

    const float _FInf = 0;
    const float _Inf = 0;
    const float _FNan = 0;
    const float _Nan = 0;
    const float _FSnan = 0;
    const float _Snan = 0;*/
}