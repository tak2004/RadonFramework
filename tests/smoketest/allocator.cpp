#include <doctest/doctest.h>
import rf.core;

TEST_CASE("Allocator stack.") {
  rf::u64 t;
  rf::Heap rootAllocator;
  //rf::Diagnostic diagnosticAllocator;
  //diagnosticAllocator.embed(&rootAllocator);
  //rf::Partion 
  //rf::Sparse sparseAllocator;
  //sparseAllocator.embed(&diagnosticAllocator);
  //rf::Dense denseAllocator;
  //denseAllocator.embed(&diagnosticAllocator);
}